type Query {
  sortSellers(
    slug: String
    identifier: ProductUniqueIdentifier
    salesChannel: Int
    country: String
    postalCode: String
  ): SortedSellers @cacheControl(scope: SEGMENT, maxAge: SHORT) @withSegment
}
